# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# react
- Use Framer Motion for animations in React components. Confidence: 0.50

# framer-motion
- Use `as const` on variant definition objects to avoid TypeScript errors with the `ease` property (string not assignable to Easing). Confidence: 0.70

